Update "Candidatos" section

- Make the Estado and CV dropdown menus shorter so the "Incluir inactivos" (label + checkbox, that should be standard size) fits on the RHS taking the same width as any of those two. Finally, aligned to RHS, the "Limpiar" button will placed.
- "Registros por página" will be moved the bottom of the result list

Therefore, the filter bar should be resized to have less height.

Also candidates that were set to "Baja lógica" should be able to get an "Alta lógica" likewise, as currently the "Baja lógica" status cannot currently be reverted and it should be.
