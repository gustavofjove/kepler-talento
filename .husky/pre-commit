#!/bin/sh

# The npm project lives in frontend/, but lint-staged runs from the root so the root
# .lintstagedrc.json applies to staged files anywhere in the repo.
PATH="$(pwd)/frontend/node_modules/.bin:$PATH" lint-staged
